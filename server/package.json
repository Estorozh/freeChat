{
  "name": "chat-app",
  "version": "0.1.0",
  "description": "",
  "license": "ISC",
  "scripts": {
    "start": "npx webpack-dev-server --mode development --hot --open --port 3000 & node server/server.js",
    "serve": "node server/server.js",
    "front": "npx webpack-dev-server --mode development --hot --port 3000",
    "build": "webpack --mode production",
    "lint": "npx eslint --ext .js,.jsx --fix ./"
  },
  "devDependencies": { },
  "dependencies": {
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "socket.io": "^2.3.0"
  }
}
